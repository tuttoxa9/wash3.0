# Workspace Rules

- **Git Push Constraint**: After successfully implementing a change and verifying that it compiles/builds successfully, always commit and push the changes (`git add`, `git commit`, and `git push`).
