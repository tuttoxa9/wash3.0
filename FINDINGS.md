# Результаты аудита BelAutoCenter

## 1. Логические ошибки в формулах

### ❌ Несоответствие флага «Минималки»
*   **Проблема:** В `HomePage.tsx` (главная страница) минимальная гарантированная оплата включена по умолчанию (если не выбрано иное). В `ReportsPage.tsx` (отчеты) она выключена по умолчанию.
*   **Результат:** Разные цифры зарплаты в ежедневной ведомости и в общем отчете за этот же день.
*   **Рекомендация:** Стандартизировать значение по умолчанию (обычно `true`) во всем приложении.

### ❌ Ошибка расчета за период (неделя/месяц)
*   **Проблема:** Отчет за период берет последнюю найденную роль сотрудника и применяет её ко всем записям периода.
*   **Результат:** Если сотрудник 2 дня был мойщиком, а 1 день админом — отчет за 3 дня посчитает его как админа за все дни (или наоборот), что приведет к неверной сумме ЗП.
*   **Рекомендация:** Переписать логику `ReportsPage` на посуточный расчет с последующим суммированием.

## 2. Технический аудит бэкенда (Supabase)

### ⚠️ Избыточность данных
*   **Проблема:** Таблица `daily_reports` хранит копию всех записей за день в колонке `records` (тип `jsonb`). При этом те же записи лежат в таблице `car_wash_records`.
*   **Риск:** Если изменить запись в одной таблице и забыть обновить другую, данные в отчетах «поплывут».
*   **Рекомендация:** В будущем отказаться от хранения массива объектов в пользу SQL-запросов (JOIN).

### ⚠️ Проблемы масштабирования
*   **Проблема:** Приложение загружает **все** предварительные записи (`appointments`) при старте.
*   **Результат:** Когда записей станет больше 1000, приложение начнет заметно тормозить при загрузке.

## 3. Безопасность и настройки

### 🔑 Хардкод пароля
*   **Проблема:** Пароль для входа в настройки (`adminlab`) прописан прямо в коде файла `SettingsPage.tsx`.
*   **Рекомендация:** Вынести пароль в переменные окружения или (лучше) использовать Supabase Auth для управления доступом.

## 4. Вердикт по формулам
Сами математические формулы (проценты, доли) в `SalaryCalculator.ts` реализованы верно и справедливо распределяют доход между участниками мойки. Основная проблема — в **данных**, которые передаются в этот калькулятор из разных страниц.

---
**Хотите, чтобы я исправил проблему с несоответствием минималки и расчетом за период прямо сейчас?**
